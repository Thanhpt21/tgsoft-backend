export enum AccountType {
  NORMAL = 'normal',
  GOOGLE = 'google',
  FACEBOOK = 'facebook',
}

export enum UserRole {
  ADMIN = 'admin',
  MANAGER = 'manager',
  STAFF = 'staff',
  USER = 'user',
}